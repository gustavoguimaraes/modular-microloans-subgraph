{
  "name": "modular-microloans-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ Modular Microloans",
    "create-local": "graph create --node http://localhost:8020/ Modular Microloans",
    "remove-local": "graph remove --node http://localhost:8020/ Modular Microloans",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 Modular Microloans",
    "test": "graph test",
    "test-full": "npm run codegen && npm run build && npm run test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.26.0",
    "@graphprotocol/graph-ts": "^0.24.1"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.0"
  }
}
